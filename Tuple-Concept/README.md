# TUPLE
tuples are immutable(meaning they cannot be changes )

# Creating a tuple

* tpl = ()
* tpl = tuple()

# Tuple Methods

* count()
counts how many times an item appears

* index()
finds the position of an item
